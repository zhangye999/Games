export function SelectIncanGold(router) {
    setTimeout(() => {
        router.push('IncanGoldIndex');
    }, 150);
}

export function SelectDaTreasure(router) {
    setTimeout(() => {
        router.push({ name: 'DaTreasureIndex' });
    }, 150);
}