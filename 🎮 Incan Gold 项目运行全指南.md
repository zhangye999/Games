# 🎮 Incan Gold 项目运行全指南
> 适配 **Windows/Mac 全新电脑** 和 **已有开发环境的开发者**，包含详细操作步骤、环境配置说明及常见问题排查。

## 📌 目录
- [一、面向开发者（已安装 Node.js + npm）](#一面向开发者已安装-nodejs--npm)
- [二、面向纯新手（全新电脑从零配置）](#二面向纯新手全新电脑从零配置)
- [三、项目启动验证 & 局域网访问](#三项目启动验证--局域网访问)
- [四、常见问题排查（必看）](#四常见问题排查必看)

---

## 一、面向开发者（已安装 Node.js + npm）
> 适用于已配置开发环境、仅需快速启动项目的场景，全程无需额外安装软件。

### 1. 克隆项目代码
打开终端（Windows 用 CMD/PowerShell，Mac 用终端），执行以下命令克隆代码：
```bash
# 克隆指定分支的代码
git clone -b Incan-Gold-Online-v2.0 https://gitee.com/Leonida/incan-gold.git

# 进入项目根目录
cd incan-gold
```

### 2. 启动后端服务
#### 步骤 1：进入后端目录
```bash
cd backend
```
#### 步骤 2：安装依赖（首次启动必做）
```bash
npm install
```
#### 步骤 3：启动后端服务
```bash
npm start
```
> ⚠️ 启动成功后，**保持终端窗口打开，不要关闭**（关闭则后端服务停止）。

### 3. 启动前端项目
#### 步骤 1：**新开一个终端窗口**（不要关闭后端终端）
#### 步骤 2：进入前端目录
```bash
# 回到项目根目录
cd ..

# 进入前端文件夹
cd frontend
```
#### 步骤 3：安装依赖（首次启动必做）
```bash
npm install
```
#### 步骤 4：启动前端服务
```bash
npm run dev
```
> ✅ 前端启动成功后，终端会输出访问地址（默认 `http://localhost:5173`）。

### 4. 访问项目
- **本地访问**：复制终端输出的地址（如 `http://localhost:5173`）到浏览器打开。
- **局域网访问**：使用电脑本机 IP + 前端端口（如 `http://192.168.10.17:5173`），手机/其他设备可通过该地址访问（需与电脑连同一 WiFi）。

---

## 二、面向纯新手（全新电脑从零配置）
> 适用于仅安装 Windows/Mac 系统、无任何开发工具的场景，按步骤逐步操作即可。

### 第一步：安装 Node.js（核心依赖，必装）
Node.js 包含运行项目所需的 `npm` 工具，安装一次永久使用。
1. 打开 Node.js 官网：https://nodejs.org/
2. 下载 **LTS 长期支持版**（推荐版本，稳定无坑）。
3. 双击安装包，**全程点击「下一步」**，无需修改任何默认设置。
4. 验证安装成功：
   - Windows：按 `Win + R` 输入 `cmd` 打开命令行，输入以下命令，出现版本号即成功：
     ```bash
     node -v
     npm -v
     ```
   - Mac：打开「终端」，输入上述命令，出现版本号即成功。

### 第二步：下载并解压项目代码
1. 打开项目仓库：https://gitee.com/Leonida/incan-gold
2. 切换到 `Incan-Gold-Online-v2.0` 分支。
3. 点击「克隆/下载」→ 选择「下载 ZIP」。
4. 下载完成后，右键 ZIP 文件选择「解压到当前文件夹」，得到项目文件夹。

### 第三步：启动后端服务
1. 打开解压后的项目根目录，进入 `backend` 文件夹。
2. **空白处按住 Shift + 右键**，选择：
   - Windows：「在此处打开命令提示符」或「在此处打开 PowerShell 窗口」。
   - Mac：「新建终端位于此处」。
3. 安装依赖（等待命令执行完成，无报错即成功）：
   ```bash
   npm install
   ```
4. 启动后端服务：
   ```bash
   npm start
   ```
> ⚠️ 此终端窗口**保持打开，不要关闭**，否则后端服务会停止。

### 第四步：启动前端项目
1. 回到项目根目录，进入 `frontend` 文件夹。
2. 同样在**空白处按住 Shift + 右键**，新建终端窗口（不要关闭后端终端）。
3. 安装依赖：
   ```bash
   npm install
   ```
4. 启动前端服务：
   ```bash
   npm run dev
   ```
> ✅ 前端启动成功后，终端会显示访问地址（如 `http://localhost:5173`）。

### 第五步：打开游戏
复制前端终端输出的地址，粘贴到浏览器中，即可开始游戏。

---

## 三、项目启动验证 & 局域网访问
### 1. 启动成功验证
- 后端终端：无报错，显示「服务启动成功」「WebSocket 监听中」等提示。
- 前端终端：无报错，显示 `Local: http://localhost:5173/` 等访问地址。
- 浏览器：能正常打开页面，点击「开始游戏」不提示「连接断开」。

### 2. 局域网手机访问教程
1. 确保电脑和手机连接**同一个 WiFi**（必须是同一网络）。
2. 查看电脑本机 IP：
   - Windows：CMD 中输入 `ipconfig`，找到「无线局域网适配器 WLAN」下的 `IPv4 地址`（如 `192.168.10.17`）。
   - Mac：终端中输入 `ifconfig`，找到 `en0` 下的 `inet` 地址（如 `192.168.10.17`）。
3. 手机浏览器输入：`http://电脑IP:前端端口`（如 `http://192.168.10.17:5173`），即可访问游戏。

---

## 四、常见问题排查（必看）
### 1. 依赖安装失败（npm install 报错）
- 原因：网络问题、Node.js 版本过低。
- 解决：
  1. 升级 Node.js 到最新 LTS 版本。
  2. 切换国内镜像加速安装：
     ```bash
     # 临时使用淘宝镜像
     npm install --registry=https://registry.npmmirror.com
     ```
  3. 删除 `node_modules` 文件夹和 `package-lock.json` 文件，重新执行 `npm install`。

### 2. 端口占用（启动服务提示「端口被占用」）
- 原因：其他程序占用了项目端口（如 3000、5173 等）。
- 解决：
  1. 使用项目自带的端口清理工具（若有）：
     ```bash
     node kill-port-servers.js
     ```
  2. 手动关闭占用端口的程序（如重启电脑、关闭浏览器、关闭其他服务）。
  3. 若需修改端口：
     - 前端：修改 `frontend/vite.config.js` 中的端口配置。
     - 后端：修改 `backend/server.js` 或配置文件中的端口。

### 3. 局域网手机无法访问
- 原因：网络不同、防火墙拦截、IP 错误。
- 解决：
  1. 确保电脑和手机连**同一 WiFi**（避免一个连 5G 一个连 2.4G）。
  2. 关闭电脑防火墙（或添加前端/后端端口到防火墙允许列表）。
  3. 重新查看电脑本机 IP，确保输入正确（无多余空格、无输错数字）。

### 4. 前端启动失败（npm run dev 报错）
- 原因：前端依赖未安装完整、Node.js 版本不兼容。
- 解决：
  1. 进入 `frontend` 文件夹，重新执行 `npm install`。
  2. 检查 `package.json` 中 `scripts` 配置是否正确（需包含 `"dev": "vite"`）。

---

## ✅ 总结
- **开发者**：克隆代码 → 前后端分别 `npm install` → 前端`npm run dev` + 后端`npm start`。
- **纯新手**：先装 Node.js → 下载代码 → 前后端分别安装依赖+启动服务。
- **核心注意**：前后端终端必须**同时打开**，局域网访问需连同一 WiFi。

按照以上步骤操作，即可快速启动并运行 Incan Gold 项目！